# Optional. If not set, app uses LOCAL MongoDB: mongodb://localhost:27017/devTinder
# To use Atlas instead (when TLS works on your network), set:
# MONGODB_URI=mongodb+srv://user:pass@namastenode.ipvudvq.mongodb.net/devTinder?retryWrites=true&w=majority&authSource=admin
# MONGODB_URI=
